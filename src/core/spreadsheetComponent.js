import {DomListener} from "@core/domListener";

/**
 *
 */
export class SpreadsheetComponent extends DomListener {
    /**
     * 
     * @return {string}
     */
    toHtml() {
        return "";
    }
}
